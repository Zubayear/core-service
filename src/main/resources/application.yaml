spring:
  profiles:
    active: @activeProfile@
  datasource:
    url: jdbc:postgresql://localhost:5432/elysium_dev
    username: dev_user
    password: jpbsL4L18kWrT
    driver-class-name: org.postgresql.Driver
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
springdoc:
  api-docs:
    path: /api-docs
management:
  endpoints:
    web:
      exposure:
        include: health